# --- linked into the shared cache, NOT committed (run ./setup.sh to materialize) ---
/sdk
/bin/thymercli
/examples

# deps / build output — except each built bundle, the only pasteable forms
node_modules/
dist/*
!dist/plugin.js
installer/dist/*
!installer/dist/plugin.js

# browser-automation output / OS
.playwright-mcp/
.DS_Store
